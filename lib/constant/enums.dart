enum APIType {
  getAccount,
  getLedger,
  getAccountResources,
  getResourcesByType,
  getAccountModules,
  getAccountModuleByID,
}
enum APIErrorType {
  unauthorized,
  badRequest,
  unknown,
}
enum LogStatus { hide, show }
